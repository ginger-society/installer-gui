apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: testing-pipe-2
  namespace: tasks-ginger-installer
  annotations:
    x-ginger-task-namespace: "ginger-installer"  # Custom annotation
    x-ginger-task-trigger-branch: '["refs/heads/master", "refs/heads/dev"]'  # Allowed branches

spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        echo "Hello, World! and here we will build the project and upload the artifacts to a bucket"
